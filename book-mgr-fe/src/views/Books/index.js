import { defineComponent } from 'vue';

export default defineComponent({
    setup() {

        const columns = [
            {

            }
        ];
        const dataSource = [];
        return {
            columns,
            dataSouce,
        }
    },
});
